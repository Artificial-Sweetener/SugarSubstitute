###############################################
# Sugar Substitute (GUI) requirements (no ComfyUI)
# Uses PyPI simple index packages for installable runtime dependencies.
###############################################

# Lower bounds are versions verified by native CI. Upper bounds admit compatible
# updates without accepting an unreviewed breaking release.

# UI toolkit and widgets
PySide6>=6.11.1,<6.12.0
PySide6-Fluent-Widgets[full]>=1.11.2,<2.0.0
PySideSix-Frameless-Window>=0.8.1,<1.0.0
qpane[mask,sam]>=2.0.4,<3.0.0

# Networking and IO
requests>=2.34.2,<3.0.0
websocket-client>=1.9.0,<2.0.0  # Provides the `websocket` import; do NOT add the legacy `websocket` package
keyring>=25.7.0,<26.0.0

# Imaging
Pillow>=12.3.0,<13.0.0

photoshop>=0.21.9,<0.22.0; sys_platform == "win32"
comtypes>=1.4.16,<2.0.0; sys_platform == "win32"
pyobjc-core>=12.2.1,<13.0.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=12.2.1,<13.0.0; sys_platform == "darwin"
pyenchant>=3.3.0,<4.0.0; sys_platform != "win32"

# Windows accent color helper (used on Windows only)
winaccent>=2.1.0,<3.0.0; sys_platform == "win32"
pygit2==1.19.3
py7zr==1.1.3
