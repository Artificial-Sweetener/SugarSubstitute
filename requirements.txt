###############################################
# Sugar Substitute (GUI) requirements (no ComfyUI)
# Uses PyPI simple index packages for installable runtime dependencies.
###############################################

# Exact direct dependency versions are validated by the project gates.

# UI toolkit and widgets
PySide6==6.11.1
PySide6-Fluent-Widgets[full]==1.11.2
PySideSix-Frameless-Window==0.8.1
qpane[mask,sam]==2.1.1

# Networking and IO
requests==2.34.2
truststore==0.10.4
websocket-client==1.9.0  # Provides the `websocket` import; do NOT add the legacy `websocket` package
keyring==25.7.0

# Imaging
Pillow==12.3.0

photoshop==0.21.9; sys_platform == "win32"
comtypes==1.4.16; sys_platform == "win32"
pyobjc-core==12.2.1; sys_platform == "darwin"
pyobjc-framework-Cocoa==12.2.1; sys_platform == "darwin"
pyenchant==3.3.0; sys_platform != "win32"

# Windows accent color helper (used on Windows only)
winaccent==2.1.0; sys_platform == "win32"
pygit2==1.19.3
py7zr==1.1.3
psutil==7.2.2
