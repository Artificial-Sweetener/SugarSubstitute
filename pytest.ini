[pytest]
testpaths = tests
addopts = -q --strict-markers --basetemp=.pytest-tmp
markers =
    platforms(*names): run a test only on the named supported operating systems
    serial: exclude a test from xdist and run its module in an isolated process
