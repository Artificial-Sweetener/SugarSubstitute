[pytest]
testpaths = tests
addopts = -q --strict-markers --import-mode=importlib
markers =
    platforms(*names): run a test only on the named supported operating systems
    isolated: run a module in a fresh non-xdist process with bounded concurrency
    serial: run a module alone after all concurrently safe test lanes
